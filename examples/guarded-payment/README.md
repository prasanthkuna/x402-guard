# Guarded payment example

Runs a local demo of allow + block flows without a live x402 signer.

```powershell
cd examples/guarded-payment
npm run start
```

Output includes `guard.lastReceipt` and `exportAuditJsonl()` hash chain.
